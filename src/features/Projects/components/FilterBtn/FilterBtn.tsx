'use client';

import type { FC } from 'react';
import { FilterBtnStyled } from './Styled';

type PropsType = {
  
};

const FilterBtn: FC<PropsType> = ({ }) => {
  return (
    <FilterBtnStyled>
	
    </FilterBtnStyled>
  );
};

export default FilterBtn;
