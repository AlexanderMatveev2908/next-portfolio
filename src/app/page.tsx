import Hero from "@/features/Hero/page";
import type { FC } from "react";

const Home: FC = () => {
  return (
    <div className="w-full pt-[20px] sm:pt-[35px]">
      <Hero />
    </div>
  );
};

export default Home;
